<!--
  android/app/src/main/AndroidManifest.xml — Chat 18 eklemeleri

  AŞAĞIDAKİ permission satırlarını <manifest> tag'i içine, <application>
  tag'inden ÖNCE ekle. <receiver> bloklarını <application> içine ekle.

  ÖNEMLİ:
  - POST_NOTIFICATIONS → Android 13 (API 33) runtime permission
  - SCHEDULE_EXACT_ALARM → Android 12+ exact alarm (Doze bypass için)
  - USE_EXACT_ALARM → Android 13+ — alternatif (galeri/calendar gibi
    "user-facing" uygulamalar için, izin gerekmez ama Play Store policy
    daha katı). Bizim uygulama "user-facing" sayıldığı için bunu kullan.
  - RECEIVE_BOOT_COMPLETED → cihaz reboot olunca alarmlar tekrar
    register edilebilsin (FLN otomatik halleder ama izin gerekli).
  - VIBRATE → notification vibration
  - WAKE_LOCK → ekranı uyandır
-->

<!-- 1. <manifest> içine, <application>'dan ÖNCE -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />

<!-- 2. <application> içine ekle (FLN'nin reboot sonrası schedule restore'u için) -->
<receiver
    android:exported="false"
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
<receiver
    android:exported="false"
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
    </intent-filter>
</receiver>
