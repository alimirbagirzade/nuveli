# pubspec.yaml — Chat 18'de eklenecek paketler
# Mevcut pubspec.yaml'a SADECE aşağıdaki satırları ekle.

dependencies:
  # ... mevcut paketler kalsın ...

  # Chat 18 — Notifications
  flutter_local_notifications: ^17.2.4
  timezone: ^0.9.4
  flutter_timezone: ^3.0.1
  permission_handler: ^11.3.1
  # shared_preferences zaten Chat 1'de eklenmiş olmalı (mock data için):
  # shared_preferences: ^2.2.2

# Komutlar:
#   cd ~/Development/nuveli
#   flutter pub add flutter_local_notifications timezone flutter_timezone permission_handler
#   flutter pub get
#
# iOS için (Podfile'a otomatik düşer, ama bir kez bunu çalıştır):
#   cd ios && pod install && cd ..
