<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera (yemek fotoğrafı çekme) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Photo library (galeriden yemek fotoğrafı seçme) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Reminders use inexact scheduling (AndroidScheduleMode.inexactAllowWhileIdle
         in notification_service.dart), so exact-alarm permissions are not needed.
         Strip them (incl. any merged in by flutter_local_notifications) to avoid
         Google Play's restricted-permission review for USE_EXACT_ALARM. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Phone health-data import (opt-in) via Google Health Connect.
         READ-only: we never write back to the user's health store. We request
         ONLY what the app actually reads + displays: workouts (READ_EXERCISE)
         and their device active-energy figure (READ_ACTIVE_CALORIES_BURNED —
         DISPLAY-ONLY, never added to the calorie budget). Steps are NOT read,
         so READ_STEPS / ACTIVITY_RECOGNITION are intentionally absent (an
         unused Health Connect permission triggers Play review rejection). -->
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />

    <application
        android:label="nuveli"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Health Connect permissions-rationale entry point. When the
                 user taps Nuveli inside the Health Connect app, this routes
                 back into the app so it can show why health data is requested
                 (opt-in import). Required by the `health` plugin / Google
                 review for sensitive health permissions. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
            <!-- Custom scheme deep links: nuveli://route. autoVerify is
                 a no-op for non-https schemes, kept here for documentation.
                 Every incoming URI MUST still flow through DeepLinkValidator
                 (lib/core/routing/) before reaching the router. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nuveli" />
            </intent-filter>

            <!-- Verified Android App Links: https://nuveli.com.tr/*.
                 autoVerify="true" tells Android to fetch
                 https://nuveli.com.tr/.well-known/assetlinks.json on install
                 and confirm the SHA-256 fingerprint of the running APK
                 matches the one published there. When verification passes,
                 https://nuveli.com.tr/... links open the app directly —
                 no chooser dialog, no browser fallback.
                 Both www and apex hosts are listed so links from either
                 are intercepted. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="nuveli.com.tr" />
                <data android:host="www.nuveli.com.tr" />
            </intent-filter>
        </activity>

        <!-- Health Connect permission-usage screen. Lets the system / Health
             Connect deep-link into the app's privacy-policy / rationale via
             MainActivity. exported + START_VIEW_PERMISSION_USAGE permission
             are required by the platform; the SAMPLES category scopes it to
             health data. See the `health` plugin Android setup. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="androidx.health.category.SAMPLES" />
            </intent-filter>
        </activity-alias>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Health Connect package visibility: lets the app detect whether the
             Health Connect app is installed and resolve its permissions-
             rationale intent. Required by the `health` plugin on Android. -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
