# Build Configs

**Durum:** ⏳ KISIM D'de doldurulacak

Bu klasör için:
- `pubspec_final.yaml` — version 1.0.0+1 + tüm prod paketler
- `Info.plist.snippet.xml` — iOS permissions
- `AndroidManifest.snippet.xml` — Android permissions
- `build.gradle.kts.snippet` — Signing config
- `keystore_setup.md` — Keystore oluşturma + yedekleme
- `flutter_native_splash.yaml` — Splash config (KISIM A'da referans verildi)
- `gitignore_additions.txt` — .gitignore eklemeleri

**Sıradaki:** KISIM D
